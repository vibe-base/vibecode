apiVersion: v1
kind: Service
metadata:
  name: localhost-service-direct-fix3
  namespace: vibecode
spec:
  selector:
    app: simple-express
  ports:
  - port: 8000
    targetPort: 5000
  type: ClusterIP
